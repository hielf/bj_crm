<div>
	<div class="well well-small">
		<h3><p class="lead text-info text-center">通知列表</p></h3>
		<table class="table table-condensed table-striped table-bordered">
			<thead>
				<tr>
					<th>
						<h5>内容</h5>
					</th>
					<th>
						<h5>提交人</h5>
					</th>
					<th>
						<h5>客户</h5>
					</th>
					<th>
						<h5>提交时间</h5>
					</th>
					<th>
						<h5>通知类型</h5>
					</th>
					<th></th>
				</tr>
			</thead>
		  <tbody>
				<% @notices.each do |n| %>
					<% case n.note_type %>
					<% when get_dict('noteType', 1).id %>
					<tr>
						<td><%= n.note %></td>
						<td><%= User.find(n.user_id).name %></td>
						<td><%= link_to Cust.find(n.cust_id).fullname, custloan_path(n.custloan_id) %></td>	
						<td><%= I18n.localize(n.updated_at, :format => :short) %></td>
						<td><%= Dict.find(n.note_type).name %></td>
						<td>
							<a href="#myModal<%=n.id%>" role="button" class="btn btn-mini pull-right" data-toggle="modal">查看</a>
						</td>	
						<%= render 'notices/notice_modal_2', :n => n %>
					</tr>
					<% when get_dict('noteType', 3).id %>
					<tr>
						<td><%= n.note %></td>
						<td><%= User.find(n.user_id).name %></td>
						<td><%= link_to Cust.find(n.cust_id).fullname, cust_path(n.cust_id) %></td>	
						<td><%= I18n.localize(n.updated_at, :format => :short) %></td>
						<td><%= Dict.find(n.note_type).name %></td>
						<td>
							<a href="#myModal<%=n.id%>" role="button" class="btn btn-mini pull-right" data-toggle="modal">查看</a>
						</td>	
						<%= render 'notices/notice_modal_3', :n => n %>
					</tr>
					
					<% when get_dict('noteType', 5).id %>
					<tr>
						<td><%= n.note %></td>
						<td><%= User.find(n.user_id).name %></td>
						<td><%= link_to Cust.find(n.cust_id).fullname, cust_path(n.cust_id) %></td>	
						<td><%= I18n.localize(n.updated_at, :format => :short) %></td>
						<td><%= Dict.find(n.note_type).name %></td>
						<td>
							<a href="#myModal<%=n.id%>" role="button" class="btn btn-mini pull-right" data-toggle="modal">查看</a>
						</td>	
						<%= render 'notices/notice_modal_5', :n => n %>
					</tr>
					
					<% when get_dict('noteType', 4).id %>
					<tr>
						<td><%= n.note %></td>
						<td><%= User.find(n.user_id).name %></td>
						<td><%= link_to Cust.find(n.cust_id).fullname, cust_path(n.cust_id) %></td>	
						<td><%= I18n.localize(n.updated_at, :format => :short) %></td>
						<td><%= Dict.find(n.note_type).name %></td>
						<td>
							<a href="#myModal<%=n.id%>" role="button" class="btn btn-mini pull-right" data-toggle="modal">查看</a>
						</td>	
						<%= render 'notices/notice_modal_4', :n => n %>
					</tr>
					<% end %>
				<% end %>
		  </tbody>
		</table>
	</div>
</div>

