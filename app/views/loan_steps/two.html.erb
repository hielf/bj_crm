<div class="row-fluid">
	<div class="span9">
		<div class="well">
			<p class="lead text-center"><legend><strong>选择担保方式</strong></legend></p>
			<%= simple_form_for @custloan,:url => wizard_path, :method => :put, :html => { :class => 'form-horizontal' } do |f| %>
				<%= render 'two_fields', :f => f %>
				<div class="form-actions">
					<%= f.submit "下一步", :class => 'btn btn-primary' %>
				</div>
			<% end %>
		</div>
	</div>
	<div class="span3">
		<%= render 'loan_steps/helps/two_helps' %>
	</div>
</div>