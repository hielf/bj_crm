<%= f.simple_fields_for :usrrels do |p| %>
	<%= p.input :mgr, as: :hidden, input_html: { value: @user.id } %>
	<%= p.input :usr, :collection => @userlist, :label => '下级用户',
							:input_html => { :multiple => true } %>
<% end %>